feat: add Navigation and Footer matching main site

Add adapted Navigation (mega-menu dropdowns) and Footer components
that replicate the main dubbingai.io site appearance. All nav links
point to the main site (https://dubbingai.io/...) since the blog is
a separate deployment. The logo links back to blog home (/).

Removed next-intl and LanguageSwitcher dependencies since blog is
English-only. Hardcoded all navigation labels.

Also fix Breadcrumbs links to use / instead of /blog since the blog
app's home route is /, not /blog.
